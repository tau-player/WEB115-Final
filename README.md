Solitare (Klondite) using the html <canvas> tag to draw most of the background/card slots.

Features:
  basic solitare turn one or turn 3
  maybe double or tripple (strech goal)
  score counter
  time played
  click and drag cards
  animations (card flipping, drawing, etc)
  undo button
  reset button

How:
  cards are img elements (based on decks I own)

  dealhand(){
    deals a randomized solitare hand
    }
  timer(){
    on load of webpage starts the timer
    }
  scorecounter(){
    shows the current score --> updates based on most recent action
    }
  Undo button(){
    on press undos the most recent move
    }
  reset button(){
    on
  
